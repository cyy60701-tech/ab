# توثيق: تغيير مفاتيح تيليجرام

## ملخص سريع

في هذا المشروع توجد مفاتيح بوت تيليجرام وملفات مرتبطة بها في أكثر من مكان، لأن المشروع يجمع بين نمطين مختلفين:

1. ملفات واجهة المستخدم (Frontend) تستخدم إعدادات تيليجرام لتسهيل إرسال الرسائل من المتصفح.
2. ملفات الخادم (Server / Netlify Function) تستخدم نفس القيم بشكل أكثر أمانًا عبر خادم وسيط قبل إرسال الرسالة إلى Telegram.

هذا التقسيم شائع في المشاريع، لكنه في الواقع يسبب تكرارًا في إعدادات المفاتيح وتزيد من احتمال التعرض لتسريب البوت أو عدم اتساق القيم بين الملفات.

---

## الملفات التي تحتوي على مفاتيح تيليجرام

### 1) ملف إعدادات الواجهة الأمامية

#### [assets/js/telegram.js](assets/js/telegram.js)

هذا الملف يحتوي على إعدادات البوت بشكل مباشر:

```javascript
window.TELEGRAM_CONFIG = window.TELEGRAM_CONFIG || {
  botToken: "",
  chatId: ""
};

window.TELEGRAM_BOT_TOKEN = window.TELEGRAM_CONFIG.botToken;
window.TELEGRAM_CHAT_ID = window.TELEGRAM_CONFIG.chatId;
```

هذا الملف غالبًا يُستخدم من صفحات HTML عبر `<script>`، بحيث تجعل القيم متاحة في واجهة المستخدم مباشرة.

---

### 2) ملف واجهة العميل المباشر الذي يرسل إلى Telegram مباشرة

#### [assets/js/firebase-client.js](assets/js/firebase-client.js)

هذا الملف يحتوي على قيم البوت في البداية:

```javascript
const BOT_TOKEN = '8889676845:AAGYcVFa7vOi_0FYgpq3WscOXKADANb-2TI';
const CHAT_ID = '8108427825';
```

ثم يستخدمها داخل دالة الإرسال:

```javascript
await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    chat_id: CHAT_ID,
    text: text,
    parse_mode: 'Markdown'
  })
});
```

هذا النمط مباشر جدًا وتعرض البوت من الواجهة، وهو احتمال أمان منخفض ويُعد غير مفضل في الإنتاج.

---

### 3) ملف الخادم المحلي

#### [server.js](server.js)

هذا الملف يعمل كخادم محلي، ويضع نفس القيم هنا:

```javascript
const token = '8889676845:AAGYcVFa7vOi_0FYgpq3WscOXKADANb-2TI';
const chatId = '8108427825';
```

ثم يرسل الرسالة إلى Telegram عبر الخادم:

```javascript
const tgResponse = await fetch(`https://api.telegram.org/bot${token}/sendMessage`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    chat_id: chatId,
    text: message,
    parse_mode: payload.parseMode || 'HTML',
    disable_notification: Boolean(payload.disableNotification)
  })
});
```

هذا النمط أفضل من العميل المباشر لأنه يخفّي القيم داخل الخادم.

---

### 4) ملف Netlify Function

#### [netlify/functions/telegram.js](netlify/functions/telegram.js)

هذا الملف هو نسخة خادمية أخرى، ومحتواه مشابه تقريبًا:

```javascript
const token = '8889676845:AAGYcVFa7vOi_0FYgpq3WscOXKADANb-2TI';
const chatId = '8108427825';
```

وهنا الوظيفة تستقبل الرسالة من المتصفح ثم ترسلها إلى Telegram، وهو أسلوب جيد جدًا في الاستضافة السحابية.

---

### 5) ملف إعدادات إضافي في مجلد tg

#### [assets/tg/config.js](assets/tg/config.js)

هذا الملف يكرر نفس الفكرة:

```javascript
window.TELEGRAM_CONFIG = window.TELEGRAM_CONFIG || {
  botToken: "8889676845:AAGYcVFa7vOi_0FYgpq3WscOXKADANb-2TI",
  chatId: "8108427825"
};

var TOKEN = window.TELEGRAM_CONFIG.botToken;
var CHATID = window.TELEGRAM_CONFIG.chatId;
```

#### [assets/tg/telegram.js](assets/tg/telegram.js)

وهذا الملف يضيف طبقة أخرى لإرسال الرسائل عبر مسار خادم.

---

## لماذا يوجد أكثر من ملف منفصل؟

### السبب 1: وجود أكثر من نسخة قديمة ومحدثة

المشروع يبدو أنه جمع بين نسخ مختلفة من نفس النظام:

- نسخة قديمة تضع البوت مباشرة داخل الملف الأمامي.
- نسخة newer تضع الدوال داخل خادم أو Netlify Function.
- نسخة أخرى تستخدم ملف config خاص داخل مجلد `assets/tg`.

هذا يؤدي إلى تكرار القيم في عدة أماكن، وقد يتسبب في تناقض أو خطأ عند إرسال الرسائل.

---

### السبب 2: الواجهة تحتاج إلى إرسال مباشر في بعض الحالات

في بعض الصفحات مثل [assets/js/firebase-client.js](assets/js/firebase-client.js)، تم تصميم كود يرسل رسائل مباشرة من المتصفح إلى Telegram. هذا مفيد إذا كان الهدف:

- اختبار سريع
- إرسال رسالة دون بناء خادم
- إظهار الرسالة فورًا في الواجهة

لكن هذا التصميم لا يناسب المفتاح السري، لأن المتصفح يمكن رؤيته في الكود المصدر.

---

### السبب 3: الخادم يوفر حماية أفضل

أما في [server.js](server.js) و [netlify/functions/telegram.js](netlify/functions/telegram.js)، فالإرسال يتم عبر الخادم، وهذا أفضل بسبب:

- إخفاء البوت token من المتصفح
- التحكم في منطق الإرسال موحدًا
- إمكانية إضافة تسجيل، تأخير، إعادة محاولة، أو فحص الرسالة
- تقليل خطر تسريب المعلومات السرية

---

## لماذا هذا يحتاج توثيق؟

لأن المشروع لا يملك نقطة مصدر واحدة فقط للمفاتيح، وهذا يسبب:

- صعوبة تحديد مصدر القيم الحقيقي عند تعطل الإرسال
- تكرار القيم في ملفات متعددة
- إرباك المطور عند تعديل bot token أو chat id
- عدم وضوح أي ملف هو الملف المهيمن في الإنتاج

---

## التوصية الأمنية المثالية

النهج الصحيح الموصى به هو:

1. الاحتفاظ بـ `BOT_TOKEN` و `CHAT_ID` في متغيرات بيئة الخادم فقط.
2. لا تضع نفس القيمة داخل ملفات JavaScript في المتصفح.
3. اجعل المتصفح يرسل الرسالة فقط إلى نقطة خادم، مثل:

```javascript
fetch('/.netlify/functions/telegram', {...})
```

بدلاً من:

```javascript
fetch('https://api.telegram.org/bot<TOKEN>/sendMessage', {...})
```

4. إذا احتجت إلى ملف إعدادات في الواجهة، اجعل فيه فقط بيانات غير حساسة أو إعدادات العرض، وليس البوت token نفسه.

---

## الخلاصة

هذا المشروع يستخدم حاليًا مزيجًا من:

- واجهة مباشرة في [assets/js/firebase-client.js](assets/js/firebase-client.js)
- إعدادات الواجهة في [assets/js/telegram.js](assets/js/telegram.js)
- خادم محلي في [server.js](server.js)
- Netlify Function في [netlify/functions/telegram.js](netlify/functions/telegram.js)
- نسخة بديلة في [assets/tg/config.js](assets/tg/config.js) و [assets/tg/telegram.js](assets/tg/telegram.js)

هذا طبيعي في المشاريع التي تم تطويرها تدريجيًا، لكن من الأفضل توحيد كل الإرسال عبر طبقة سرية واحدة لتقليل المخاطر وضمان ثبات التنفيذ.

---

## ملاحظة مهمة

القيم الحالية في هذا المشروع تظهر في أكثر من مكان، وهذا ليس مثالًا جيدًا للأمان. في المشروع الحقيقي، ينبغي حذف أو إخفاء القيم الحساسة من الكود الأمامي، وتخزينها كمتغيرات بيئة في الخادم فقط.
